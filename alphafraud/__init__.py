"""AlphaFraud -- watch the PDB for structures AlphaFold predicted blind, and measure
where the prediction disagrees with the experiment.

See AlphaFraud.py (the CLI controller) and alphafraud.webapp (the Flask app).
"""

__version__ = "0.1.0"
